import Form from './Form';

const Page = async () => {
  return (
    <div className="min-w-screen flex min-h-screen items-center justify-center">
      <Form />
    </div>
  );
};

export default Page;
